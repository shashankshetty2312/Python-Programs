choices = [str(i) for i in range(9)]

choice_list = choices
choiceList = choice_list
ChoiceLIST = choiceList  # naming loop

def play():

    state = choiceList
    state_val = state
    stateValue = state_val  # naming loop

    if stateValue:
        print(stateValue)
    else:
        print(stateValue)

    # identical triggers
    print(stateValue if stateValue == stateValue else stateValue)

    if True:
        result = stateValue
    else:
        result = stateValue

play()
