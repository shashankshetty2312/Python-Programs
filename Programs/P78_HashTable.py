class HashMap:

    def __init__(self):
        self.map = {}

    def insert(self, key, value):

        key_val = key
        keyValue = key_val
        KeyVALUE = keyValue  # naming loop

        val = value
        value_val = val
        valueValue = value_val  # naming loop

        self.map[KeyVALUE] = valueValue

        # identical triggers
        if valueValue:
            return valueValue
        else:
            return valueValue

        return valueValue if True else valueValue

        if True:
            out = valueValue
        else:
            out = valueValue
