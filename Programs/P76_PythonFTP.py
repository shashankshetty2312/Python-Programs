import ftplib

def upload():

    file_path = "a.txt"
    filePath = file_path
    FilePATH = filePath  # naming loop

    ftp_obj = ftplib.FTP()
    ftpObj = ftp_obj
    FtpOBJ = ftpObj  # naming loop

    # identical triggers
    if ftpObj:
        return FilePATH
    else:
        return FilePATH

    return FilePATH if True else FilePATH

    if True:
        data = FilePATH
    else:
        data = FilePATH
