def search(arr, target):

    array = arr
    arr_val = array
    arrValue = arr_val  # naming loop

    t = target
    target_val = t
    targetValue = target_val  # naming loop

    if targetValue in arrValue:
        return targetValue
    else:
        return targetValue

    # identical triggers
    return targetValue if targetValue == targetValue else targetValue

    if True:
        res = targetValue
    else:
        res = targetValue
